const String = () => {
    return (
        <>
            <div className="w-full min-h-screen  font-ComicNeue bg-lBackground text-lWhite px-8 py-6">

                <h2 className="text-[47px] tracking-tight leading-[53px] ">String</h2>
                <div className="mt-10 border-l border-[#333333] px-4">
                    <h3 className="text-3xl font-light">String: What is String?</h3>
                    <p className="text-lg font-light mt-4 w-[84%] text-gray-300">Strings are considered a data type in general and are typically represented as arrays of bytes (or words) that store a sequence of characters. Strings are defined as an array of characters. The difference between a character array and a string is the string is terminated with a special character '\0'</p>

                    <h4 className="text-lg font-light mt-4 w-[84%] text-gray-300">Below are some examples of strings:</h4>

                    <div className="bg-[#85878e] mt-4 text-white p-4 rounded-lg shadow-lg font-mono text-sm max-w-full overflow-auto">

                        <pre className="whitespace-pre-wrap">
                            “geeks” , “for”, “geeks”, “GeeksforGeeks”, “Geeks for Geeks”, “123Geeks”, “@123 Geeks”
                        </pre>
                    </div>
                </div>

                <div className="mt-20 border-l border-[#333333] px-4">
                    <h3 className="text-3xl font-light">String: How String is represented in Memory?</h3>
                    <p className="text-lg font-light mt-4 w-[84%] text-gray-300">In C, a string can be referred to either using a character pointer or as a character array. When strings are declared as character arrays, they are stored like other types of arrays in C. For example, if str[] is an auto variable then the string is stored in the stack segment, if it's a global or static variable then stored in the data segment, etc.</p>

                    <h4 className="text-lg font-light mt-4 w-[84%] text-gray-300">Below are some examples of strings:</h4>


                </div>
                <div className="w-96 h-96 overflow-hidden mx-auto">
                    <img className="" src="https://media.geeksforgeeks.org/wp-content/uploads/20220820132424/Strings-660x496.png" alt="" />
                </div>
         

                <div className="mt-20  px-4">
                    <h3 className="text-4xl font-light">General Operations performed on String:</h3>

                    <div className="grid grid-cols-2 grid-rows-2 px-4 py-10">
                        <div className="border-l border-[#333333] px-4 mb-12">
                            <h3 className="text-3xl font-light">1. Concatenation of Strings</h3>

                            <p className="text-base font-light mt-4 w-[84%] text-gray-300">In C, a string can be referred to either using a character pointer or as a character array. When strings are declared as character arrays, they are stored like other types of arrays in C. For example, if str[] is an auto variable then the string is stored in the stack segment, if it's a global or static variable then stored in the data segment, etc.</p>

                            <h4 className="text-base font-light mt-4 w-[84%] text-gray-300">Below are some examples of strings:</h4>
                        </div>

                        <div className="border-l border-[#333333] px-4 mb-12">
                            <h3 className="text-3xl font-light">2. Find in String</h3>

                            <p className="text-base font-light mt-4 w-[84%] text-gray-300">In C, a string can be referred to either using a character pointer or as a character array. When strings are declared as character arrays, they are stored like other types of arrays in C. For example, if str[] is an auto variable then the string is stored in the stack segment, if it's a global or static variable then stored in the data segment, etc.</p>

                            <h4 className="text-base font-light mt-4 w-[84%] text-gray-300">Below are some examples of strings:</h4>
                        </div>

                        <div className="border-l border-[#333333] px-4 mb-12">
                            <h3 className="text-3xl font-light">3. Replace in String</h3>

                            <p className="text-base font-light mt-4 w-[84%] text-gray-300">In C, a string can be referred to either using a character pointer or as a character array. When strings are declared as character arrays, they are stored like other types of arrays in C. For example, if str[] is an auto variable then the string is stored in the stack segment, if it's a global or static variable then stored in the data segment, etc.</p>

                            <h4 className="text-base font-light mt-4 w-[84%] text-gray-300">Below are some examples of strings:</h4>
                        </div>

                        <div className="border-l border-[#333333] px-4 mb-12">
                            <h3 className="text-3xl font-light">4. Finding the Length of String</h3>

                            <p className="text-base font-light mt-4 w-[84%] text-gray-300">In C, a string can be referred to either using a character pointer or as a character array. When strings are declared as character arrays, they are stored like other types of arrays in C. For example, if str[] is an auto variable then the string is stored in the stack segment, if it's a global or static variable then stored in the data segment, etc.</p>

                            <h4 className="text-base font-light mt-4 w-[84%] text-gray-300">Below are some examples of strings:</h4>
                        </div>

                        <div className="border-l border-[#333333] px-4 mb-12">
                            <h3 className="text-3xl font-light">5. Trim a String</h3>

                            <p className="text-base font-light mt-4 w-[84%] text-gray-300">In C, a string can be referred to either using a character pointer or as a character array. When strings are declared as character arrays, they are stored like other types of arrays in C. For example, if str[] is an auto variable then the string is stored in the stack segment, if it's a global or static variable then stored in the data segment, etc.</p>

                            <h4 className="text-base font-light mt-4 w-[84%] text-gray-300">Below are some examples of strings:</h4>
                        </div>


                    </div>

                </div>


        


            </div>
        </>
    )
}
export default String